import mongoose from 'mongoose';

import { ICarManufacturer } from './types';

const instantiatedCarManufacturers: Array<ICarManufacturer> = [];

const initialize = () => { 
    
}

export default initialize;

